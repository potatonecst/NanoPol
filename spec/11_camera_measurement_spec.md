# NanoPol カメラ制御および測定・保存仕様

本仕様書は、ナノ粒子や量子ドットからの散乱光強度を正確に測定するための、カメラハードウェアの制御、データ保存フォーマット、およびストリーミング配信のアーキテクチャについて定義する。

## 1. 基本方針 (設計思想)
*   **汎用性の完全確保:**
    測定に使用する波長（785nm, 637nm等）やサンプルに依存せず、常に正確な「相対強度」を測定・規格化可能にする。
*   **ブラックボックス処理の排除:**
    人間が見やすい画像を作るためのソフトウェア的な自動色補正（ThorCam等が裏で行っているガンマ補正やホワイトバランスの自動調整）を完全に排除し、カメラを「純粋な光子カウンター」として運用する。

## 2. モード別の測定・保存仕様
システムは、ユーザーの目的に応じて「Colorモード」と「Monochrome (RAW) モード」の2つのカメラ動作モードを提供する。

### 2.1. Color モード (人間向けの確認・記録用)
*   **用途:** サンプルの色味を確認したい場合や、手軽な記録。
*   **状態別の動作:**
    *   **待機中 (プレビュー):** カメラによるデモザイク処理・補正を経たフルカラー映像で配信（最大 30fps）。
    *   **Snapshot (1枚撮影):** カメラから取得した16-bit RAWデータからデモザイク計算を行い、48-bit フルカラーTIFF（または指定フォーマット）として遅延なく保存する。
    *   **録画:** 測定中の処理落ちを防ぐため、一旦「16-bit RAW モノクロTIFF」として生データを直書き保存する。測定完了後、事後処理（貨物レーン）にて色を復元（デモザイク）し、MP4動画や48-bitカラーTIFFを生成する。

### 2.2. Monochrome / RAW モード (解析・精密測定用)
光強度の定量評価（Snapshot / Step & Shoot / 16-bit録画）を行うための本番用モード。

1.  **データ取得の基本原則:**
    カメラセンサーから `10-bit` または `16-bit RAW` データ（Bayer配列）を直接取得し、ソフトウェアによるデモザイク（補間処理）を一切行わない。
2.  **ビニング処理 (Binning):**
    多様な波長（特にRGBフィルターの境界が曖昧になる近赤外領域など）に対応するため、取得したRAWデータの **「2x2の4画素（R, G, G, B）」の数値を単純加算（ソフトウェアビニング）** し、これを1つの大きなピクセルの光強度データとする。
3.  **保存形式:**
    RGBチャネルを持たない **「1チャンネルの 16-bit TIFF」** 形式で保存する。
    *   **録画時のパフォーマンス:** 録画時はOSのファイルI/O遅延（ファイル作成のオーバーヘッドによるコマ落ち）を防ぐため、**「マルチページTIFF」** (`tifffile` ライブラリの `append=True` 機能など) を使用し、1つのファイルに全フレームを追記保存する。
    *   **命名規則:** 連番探索による遅延（ファイルが増えると探索が遅くなる問題）を排除するため、ファイル名は `<Prefix>_YYYYMMDD_HHMMSS.tif` のように**タイムスタンプベース**で一意に生成する。
4.  **ハードウェア設定の完全固定:**
    測定の比例関係（リニアリティ）を崩さないため、バックエンド側で以下の設定を強制する。
    *   `Gamma`: **1.0 に固定**（無効化）。
    *   `White Balance (AWB)`: **Autoをオフ**にし、RGBのゲインをすべて **1.0 に固定**。
    *   `Hardware Gain`: 手動で指定された値に固定。

5.  **状態別のシステム挙動:**
    *   **待機中 (プレビューのみ):**
        プレビュー配信とSnapshot保存時の遅延をゼロにするため、カメラは常に「16-bit RAWモード」で動作させ続ける。プレビュー表示用には、PC側で8-bitに圧縮（およびカラー化）した映像を配信する。
    *   **Snapshot (1枚撮影) の瞬間:**
        カメラは既に16-bitで動作しているためモード切替の遅延は発生しない。保持されている最新フレーム（`latest_frame`）から画像を1枚参照し、モノクロ設定時はそのまま16-bit TIFFとして、カラー設定時は48-bitフルカラーTIFF（または指定フォーマット）として保存する。
    *   **動画録画中:** 選択された録画モードにより動作が分岐する。
        測定中の処理落ち（コマ落ち）やメモリパンクを完全に防ぐため、録画中はリアルタイムのMP4エンコードを行わず、**すべてマルチページTIFFへの直書き**に統一する。
        *   **16-bit 録画 (本番測定用):** 用途: 画像解析を用いた正確な光強度の数値化・グラフ化。動作: カメラを16-bit RAWで回し、そのままTIFFファイルに追記し続ける（データ量は大きいがグラデーションが完璧）。
        *   **8-bit 録画 (軽量記録用):** 用途: ざっくりとした光の変化を後で見返す長時間の記録。動作: 録画開始時にカメラを「8-bit モノクロモード」に切り替え、データ量を半減させてからTIFFファイルに追記し続ける。
        *   **MP4事後変換 (Post-processing):** 測定完了後、ユーザーの設定に応じて保存されたTIFFからMP4動画を非同期で生成する。「生データ(TIFF)を残すか、変換後に自動削除するか」は設定で選択可能とする。
        *   **安全装置 (Fail-safe):** SSDの容量枯渇を防ぐため、録画フレーム数や空き容量に基づく強制停止リミッターを実装する。

## 3. バックエンド・アーキテクチャ (3スレッド負荷分散設計)
「全フレームの確実な保存」と「ストリーミング配信」を両立し、処理落ち（コマ落ち）を完全に防ぐため、バックエンド（Python）では **「3つの独立したレーン（スレッド）」** を用いた非同期設計を採用する。

1.  **特急レーン (Capture Thread):**
    *   役割: カメラから「16-bit RAWデータ」を最速で取得する。
    *   録画処理: 録画中であれば、取得した生データをそのまま「開きっぱなしの巨大なTIFFファイル」に高速で直書き（追記）する。エンコード等の重い処理は一切行わない。
    *   連携: その後、プレビュー用の黒板（`latest_frame`）に最新画像を書き込み、待機中のスレッドに一斉通知（ベル）をして、すぐに次の画像取得に戻る。
2.  **各駅停車レーン (Streaming Thread):**
    *   役割: ベル（通知）を待ち、自身のペースで黒板にある「その時の最新の画像」を参照する。
    *   処理: 時間をかけて 8-bit への変換、デモザイク（カラー化）、JPEG圧縮を行い、ブラウザへMJPEGとして送信する。複数クライアントへの同時配信も可能であり、特急レーンの動作には一切干渉しない。
3.  **貨物レーン (Post-processing / 事後処理):**
    *   役割: 録画停止（測定完了）後に起動し、保存済みの巨大なマルチページTIFFを読み込んで目的のフォーマットに変換・出力する。
    *   カラー動画の変換フロー: ユーザーがカラー出力を希望した場合、このレーンで `16-bit RAW TIFF` を読み込む → メモリ上で `デモザイク処理` を行い色を復元する → `48-bit フルカラーTIFF` または `24-bit フルカラーMP4 (8-bit x 3)` に変換して保存する。この処理をフレームごとにストリーム実行することで、ディスク容量の無駄遣いを防ぎつつ高画質なカラー記録を得る。

この設計により、**「測定データの完全性の保証」と「プレビュー映像のリアルタイム性の維持」を両立**させる。

## 4. メモリ管理とスレッド間通信 (実装方針)
マルチスレッド環境下におけるメモリアクセス違反を防ぎ、16-bit RAWデータの欠損（コマ落ち）を完全に排除するため、バックエンド（Python）では以下のメモリ管理ポリシーを厳格に適用する。専門用語が多いため、それぞれの目的と「現実世界の作業」に例えた補足を付記する。

### 4.1. ハードウェアバッファの動的割り当て
カメラがPCのRAM上に直接データを書き込むための「画像バッファ（予約席）」は、現在の状態（Idle, Snapshot, Rec）に応じて動的に再割り当てを行う。
*   **待機中 (8-bit) / 録画モードA:** 1画素あたり 1バイト (8-bit) のメモリ空間（小さな箱）を確保。
*   **Snapshot / 録画モードB:** 1画素あたり 2バイト (16-bit) のメモリ空間（大きな箱）を確保。
*   **【補足説明】:** カメラに「これから送るデータのサイズが変わるから、それに合わせた新しいキャンバス（メモリ）を用意してね」と指示する処理です。撮影ボタンを押した瞬間などに、古いキャンバスを片付けて新しいキャンバスを準備します。

### 4.2. ディープコピーによるメモリ隔離 (画像破綻の防止)
カメラ（uEye SDK）は、確保したバッファに対してハードウェアレベルで絶えず最新の画像データを上書きし続ける。ストリーミング用スレッドが処理を行っている最中にデータが上書きされて画像が破綻（ティアリング）するのを防ぐため、**「メモリの完全な複製 (Deep Copy)」** を徹底する。
*   **【補足説明】:** カメラは「超高速で同じキャンバスに何度も上書きし続ける画伯」です。もし私たちがそのキャンバスを直接覗き込みながら時間をかけて配信の準備をしていると、途中で画伯が新しい絵を上書きしてしまい、「上半分が1秒前の絵、下半分が今の絵」というようなバケモノ画像が生まれます。これを防ぐため、絵が完成した瞬間に「サッとコピー機で複製（ディープコピー）」を作り、私たちはそのコピー用紙を別の部屋に持っていって配信作業を行います。

### 4.3. スレッド間通知 (Condition / 黒板とベル方式の実現)
保存用スレッド（特急レーン）からストリーミング用スレッド（各駅停車レーン）へコピーした画像を渡す際は、**`threading.Condition` を用いた「黒板とベル方式」** を使用する。
*   **挙動 (Non-blocking):** 保存用スレッドは、最新の画像を1枚だけ保持する変数（黒板）を上書きし、待機しているすべてのスレッドに対して一斉通知（ベル）を行う。
*   **【補足説明 - なぜキューを使わないのか？】:** 初期設計の「最大サイズ1の共有キュー（トレイ）」では、誰か1人が画像を取り出すとトレイが空になり、複数画面（例えばPCとスマホ）で同時にアクセスした際に「フレームの奪い合い」によるカクつきが発生していました。「黒板とベル方式」に変更することで、画像を「奪う」のではなく「参照（コピー）」する形になり、複数クライアントへ同時に最新フレームを安定配信できるようになります。保存側は書き込んでベルを鳴らすだけで、配信側がモタついていても一切影響を受けません（処理遅延の完全分離）。

### 4.4. ガベージコレクション (GC) 負荷への備え
毎秒何十回も数MBのディープコピーを作成・破棄するため、Pythonの自動メモリ掃除機能（ガベージコレクション）による瞬間的な処理停止（マイクロスタッター）が発生するリスクがある。初期実装ではPython標準のメモリ管理に任せるが、コマ落ちが確認された場合は **「メモリプーリング」** への移行を検討する。
*   **【補足説明 - メモリプーリングとは？】:** コピー用紙を毎秒30枚も印刷してはゴミ箱に捨てていると、Pythonの清掃員（ガベージコレクション）が「ちょっと作業止めるよ！ゴミ捨てるから！」と割り込んできます。この一瞬の停止がコマ落ちの原因になります。これを防ぐ究極の対策が「メモリプーリング」です。これは紙を使うのをやめて「3枚のホワイトボードを準備し、それを消して使い回す」という手法です。ゴミが出なくなるため、清掃員が介入してくることがなくなります。まずは通常通り（毎回新しい紙を作り、古い紙は自動消滅させる方式）で作りますが、もしコマ落ちするようならこのホワイトボード方式を導入します。

### 4.5. Pythonのメモリ管理と参照カウントの活用 (技術的背景)
システムが際限なくメモリを消費（メモリリーク）せず、かつ超高速に動作する裏付けとして、Pythonの「参照」の仕組みを最大限に活用する。
*   **黒板の中身は「実体」ではなく「参照」:** スレッド間で受け渡しされるのは、数MBの画像データそのものではなく、メモリ上の位置を示す「参照（名札・ポインタのようなもの）」のみである。そのため、最新フレームの上書きは数マイクロ秒で完了する。
*   **参照の共有と自動消滅:** ストリーミングスレッドが黒板（`latest_frame`）から画像を読み取る動作は、単なる参照のコピーである。保存用スレッドが黒板に新しい画像の参照を上書きした際、もしどのストリーミングスレッドも古い参照を持っていなければ、該当画像への「参照カウント」が0になる。Pythonはこの状態を検知し、即座にメモリから実体データを完全に解放する。
*   **結果:** 毎秒30回新しい画像メモリ（約2.6MB）を確保し続けても、メモリ上に同時に存在するのは「黒板にある最新の1枚」と「各スレッドが処理中の1枚」のみに保たれ、システムのメモリパンクを完全に防ぐことができる。